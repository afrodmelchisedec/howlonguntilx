#!/usr/bin/env node
/**
 * setup-seo-pipeline.cjs
 *
 * Creates the SEO pipeline file structure inside the current repo, in place.
 * Safe to re-run: existing files are skipped (pass --force to overwrite),
 * and appends to .gitignore / prisma/schema.prisma / .env.example are
 * de-duplicated.
 *
 * Run from the repo root:
 *   node setup-seo-pipeline.cjs
 *   node setup-seo-pipeline.cjs --force   (overwrite files that already exist)
 */

const fs = require("fs");
const path = require("path");

const FORCE = process.argv.includes("--force");
const ROOT = process.cwd();

const PY_CONTENT = "#!/usr/bin/env python3\n\"\"\"\nseo_pipeline_v1.py \u2014 SEO Opportunity Hunter, v1 (DataForSEO-powered)\n\nWHAT THIS DOES\n    seed keyword(s) -> DataForSEO Labs keyword ideas -> volume/KD filter\n    -> SERP pull for the survivors (capped, cached) -> cannibalization\n    clustering by SERP overlap -> programmatic-template detection\n    -> scored Excel workbook (Opportunities / Programmatic / Raw_Keywords)\n\nSETUP\n    pip install requests openpyxl python-dotenv\n\n    Add these to .env.local at your repo root (same file Next.js reads \u2014\n    see .env.example for the placeholder block):\n        DATAFORSEO_LOGIN=your_login\n        DATAFORSEO_PASSWORD=your_password\n\n    This script auto-loads .env.local (falling back to .env) by searching\n    upward from wherever this file lives, so it works the same way no\n    matter which directory you run it from or which machine you're on.\n    For a deployed/server-triggered run, set the same two variables in\n    your host's environment variables UI (e.g. Netlify: Site settings ->\n    Environment variables) instead of a file.\n\nUSAGE\n    python seo_pipeline_v1.py --seed \"days until\" --site \"https://howlonguntilx.com\" \\\n        --country \"United States\" --language English \\\n        --min-volume 300 --max-kd 30 --max-serp-calls 40\n\nCOST CONTROL (read this before running against a real account)\n    - Every DataForSEO call is cached to disk under cache/ for CACHE_TTL_DAYS.\n      Re-running the same seed within that window costs nothing.\n    - Keyword-idea calls (cheap) run first and filter the list down.\n    - SERP calls (expensive) only run for keywords that already cleared\n      --min-volume / --max-kd, and are hard-capped by --max-serp-calls.\n    - Use --dry-run to see how many keywords/SERP calls a config would\n      trigger WITHOUT spending anything (it prints counts and exits).\n\"\"\"\n\nimport argparse\nimport base64\nimport hashlib\nimport json\nimport os\nimport re\nimport sys\nimport time\nfrom collections import defaultdict\nfrom datetime import datetime\nfrom pathlib import Path\n\nimport requests\nfrom openpyxl import Workbook\nfrom openpyxl.styles import Alignment, Font, PatternFill\nfrom openpyxl.utils import get_column_letter\n\ntry:\n    from dotenv import load_dotenv\nexcept ImportError:\n    load_dotenv = None\n\n\ndef load_env_file():\n    \"\"\"Search upward from this script's own directory for .env.local or\n    .env and load whichever is found first. Real environment variables\n    (e.g. set by Netlify, or already exported in the shell) always take\n    priority and are never overwritten.\"\"\"\n    if load_dotenv is None:\n        print(\"Note: python-dotenv not installed (pip install python-dotenv) \u2014 \"\n              \"falling back to real environment variables only.\")\n        return\n    here = Path(__file__).resolve().parent\n    for directory in [here, *here.parents]:\n        for name in (\".env.local\", \".env\"):\n            candidate = directory / name\n            if candidate.exists():\n                load_dotenv(candidate, override=False)\n                print(f\"Loaded credentials from {candidate}\")\n                return\n    # Not finding a file isn't an error \u2014 real env vars may already be set\n    # (Netlify, CI, or a manual `export` in the current shell).\n\n\nload_env_file()\n\nDATAFORSEO_BASE = \"https://api.dataforseo.com/v3\"\nCACHE_DIR = Path(\"cache\")\nCACHE_TTL_DAYS = 30\n\n# Templates we actively look for when detecting programmatic opportunities.\n# Order matters: longer/more specific patterns first so \"how many days until\"\n# isn't also matched as \"days until\".\nPROGRAMMATIC_TEMPLATES = [\n    r\"how many days until (?P<entity>.+)\",\n    r\"how many weeks until (?P<entity>.+)\",\n    r\"how many months until (?P<entity>.+)\",\n    r\"days until (?P<entity>.+)\",\n    r\"weeks until (?P<entity>.+)\",\n    r\"months until (?P<entity>.+)\",\n    r\"(?P<entity>.+) countdown\",\n]\n\n# ---------------------------------------------------------------------------\n# HTTP / caching\n# ---------------------------------------------------------------------------\n\ndef auth_header():\n    login = os.environ.get(\"DATAFORSEO_LOGIN\")\n    password = os.environ.get(\"DATAFORSEO_PASSWORD\")\n    if not login or not password:\n        sys.exit(\n            \"Missing credentials. Add DATAFORSEO_LOGIN and DATAFORSEO_PASSWORD to \"\n            \".env.local at your repo root (see .env.example), or set them as real \"\n            \"environment variables (e.g. in Netlify's dashboard for a deployed run).\"\n        )\n    token = base64.b64encode(f\"{login}:{password}\".encode()).decode()\n    return {\"Authorization\": f\"Basic {token}\", \"Content-Type\": \"application/json\"}\n\n\ndef cached_post(endpoint, payload, ttl_days=CACHE_TTL_DAYS, label=\"\"):\n    \"\"\"POST to DataForSEO with an on-disk cache keyed by endpoint+payload.\"\"\"\n    CACHE_DIR.mkdir(exist_ok=True)\n    key = hashlib.sha256((endpoint + json.dumps(payload, sort_keys=True)).encode()).hexdigest()\n    cache_file = CACHE_DIR / f\"{key}.json\"\n    if cache_file.exists():\n        age_days = (time.time() - cache_file.stat().st_mtime) / 86400\n        if age_days < ttl_days:\n            return json.loads(cache_file.read_text())\n    print(f\"  [API] {label or endpoint}\")\n    resp = requests.post(f\"{DATAFORSEO_BASE}/{endpoint}\", headers=auth_header(), json=payload, timeout=60)\n    resp.raise_for_status()\n    data = resp.json()\n    cache_file.write_text(json.dumps(data))\n    return data\n\n\n# ---------------------------------------------------------------------------\n# DataForSEO calls\n# ---------------------------------------------------------------------------\n\ndef get_keyword_ideas(seed, location_name, language_name, limit=700):\n    \"\"\"DataForSEO Labs keyword ideas: cheap, broad discovery from a seed.\"\"\"\n    payload = [{\n        \"keywords\": [seed],\n        \"location_name\": location_name,\n        \"language_name\": language_name,\n        \"limit\": limit,\n        \"include_serp_info\": False,\n    }]\n    data = cached_post(\"dataforseo_labs/google/keyword_ideas/live\", payload, label=f\"keyword_ideas: {seed}\")\n    items = []\n    for task in data.get(\"tasks\", []):\n        for result in (task.get(\"result\") or []):\n            for item in (result.get(\"items\") or []):\n                info = item.get(\"keyword_info\") or {}\n                items.append({\n                    \"keyword\": item.get(\"keyword\", \"\"),\n                    \"volume\": info.get(\"search_volume\") or 0,\n                    \"cpc\": info.get(\"cpc\") or 0,\n                    \"competition\": info.get(\"competition\") or 0,\n                    \"kd\": (item.get(\"keyword_properties\") or {}).get(\"keyword_difficulty\"),\n                    \"monthly_searches\": info.get(\"monthly_searches\") or [],\n                })\n    return items\n\n\ndef get_serp(keyword, location_name, language_name, device=\"desktop\"):\n    \"\"\"SERP live call \u2014 the expensive one. Only call for pre-filtered keywords.\"\"\"\n    payload = [{\n        \"keyword\": keyword,\n        \"location_name\": location_name,\n        \"language_name\": language_name,\n        \"device\": device,\n        \"depth\": 10,\n    }]\n    data = cached_post(\"serp/google/organic/live/advanced\", payload, label=f\"serp: {keyword}\")\n    domains = []\n    for task in data.get(\"tasks\", []):\n        for result in (task.get(\"result\") or []):\n            for item in (result.get(\"items\") or []):\n                if item.get(\"type\") == \"organic\" and item.get(\"domain\"):\n                    domains.append(item[\"domain\"])\n    return domains[:10]\n\n\n# ---------------------------------------------------------------------------\n# Scoring / trend / clustering / programmatic detection\n# ---------------------------------------------------------------------------\n\ndef trend_direction(monthly_searches):\n    \"\"\"Compare the most recent 3 months to the prior 9. Returns up/flat/down.\"\"\"\n    if not monthly_searches or len(monthly_searches) < 6:\n        return \"?\"\n    vols = [m.get(\"search_volume\") or 0 for m in monthly_searches]\n    recent = sum(vols[-3:]) / 3\n    prior = sum(vols[:-3]) / max(len(vols[:-3]), 1)\n    if prior == 0:\n        return \"?\"\n    change = (recent - prior) / prior\n    if change > 0.15:\n        return \"up\"\n    if change < -0.15:\n        return \"down\"\n    return \"flat\"\n\n\ndef opportunity_score(volume, kd, trend, has_serp):\n    \"\"\"0-100 composite score. Weighting matches the blueprint we agreed on.\"\"\"\n    import math\n    vol_score = min(100, math.log10(max(volume, 1) + 1) * 25)      # log-scaled demand\n    kd_val = kd if kd is not None else 50                            # unknown KD -> assume medium risk\n    kd_score = max(0, 100 - kd_val)\n    trend_score = {\"up\": 100, \"flat\": 60, \"down\": 20, \"?\": 50}[trend]\n    serp_confidence = 1.0 if has_serp else 0.7                       # discount unverified rows\n    score = (0.40 * vol_score + 0.35 * kd_score + 0.25 * trend_score) * serp_confidence\n    return round(score, 1)\n\n\ndef cluster_by_serp_overlap(keyword_domains, threshold=0.3):\n    \"\"\"Group keywords whose top-10 SERP domains overlap >= threshold (cannibalization check).\"\"\"\n    keywords = list(keyword_domains.keys())\n    cluster_of = {}\n    next_id = 0\n    for i, kw_a in enumerate(keywords):\n        if kw_a in cluster_of:\n            continue\n        cluster_of[kw_a] = next_id\n        set_a = set(keyword_domains[kw_a])\n        for kw_b in keywords[i + 1:]:\n            if kw_b in cluster_of or not set_a:\n                continue\n            set_b = set(keyword_domains[kw_b])\n            if not set_b:\n                continue\n            overlap = len(set_a & set_b) / len(set_a | set_b)\n            if overlap >= threshold:\n                cluster_of[kw_b] = next_id\n        next_id += 1\n    return cluster_of\n\n\ndef detect_programmatic(keyword):\n    \"\"\"Match a keyword against known templates; return (template, entity) or (None, None).\"\"\"\n    for pattern in PROGRAMMATIC_TEMPLATES:\n        m = re.fullmatch(pattern, keyword.strip(), re.IGNORECASE)\n        if m:\n            template = re.sub(r\"\\(\\?P<entity>\\.\\+\\)\", \"{entity}\", pattern).replace(\"\\\\\", \"\")\n            return template, m.group(\"entity\").strip()\n    return None, None\n\n\n# ---------------------------------------------------------------------------\n# Output workbook\n# ---------------------------------------------------------------------------\n\nHEADER_FILL = PatternFill(start_color=\"1F2937\", end_color=\"1F2937\", fill_type=\"solid\")\nHEADER_FONT = Font(bold=True, color=\"FFFFFF\")\n\n\ndef write_header(ws, headers):\n    for col, title in enumerate(headers, start=1):\n        cell = ws.cell(row=1, column=col, value=title)\n        cell.fill = HEADER_FILL\n        cell.font = HEADER_FONT\n        cell.alignment = Alignment(horizontal=\"left\")\n        ws.column_dimensions[get_column_letter(col)].width = max(14, len(title) + 4)\n    ws.freeze_panes = \"A2\"\n\n\ndef build_workbook(rows, out_path):\n    wb = Workbook()\n\n    # --- Opportunities ---\n    ws = wb.active\n    ws.title = \"Opportunities\"\n    headers = [\"Priority\", \"Keyword\", \"Volume\", \"KD\", \"Trend\", \"Cluster\", \"Opportunity\", \"Programmatic Template\", \"Notes\"]\n    write_header(ws, headers)\n    for r in sorted(rows, key=lambda x: -x[\"opportunity\"]):\n        priority = \"HIGH\" if r[\"opportunity\"] >= 80 else (\"MED\" if r[\"opportunity\"] >= 60 else \"LOW\")\n        ws.append([\n            priority, r[\"keyword\"], r[\"volume\"], r[\"kd\"] if r[\"kd\"] is not None else \"?\",\n            r[\"trend\"], r[\"cluster\"], r[\"opportunity\"], r[\"template\"] or \"\", r.get(\"notes\", \"\"),\n        ])\n\n    # --- Programmatic ---\n    ws2 = wb.create_sheet(\"Programmatic\")\n    headers2 = [\"Template\", \"Entity\", \"Keyword\", \"Volume\", \"KD\", \"Opportunity\", \"Suggested Slug\", \"Status\"]\n    write_header(ws2, headers2)\n    by_template = defaultdict(list)\n    for r in rows:\n        if r[\"template\"]:\n            by_template[r[\"template\"]].append(r)\n    for template, items in by_template.items():\n        for r in sorted(items, key=lambda x: -x[\"opportunity\"]):\n            slug = re.sub(r\"[^a-z0-9]+\", \"-\", r[\"entity\"].lower()).strip(\"-\")\n            status = \"BUILD\" if r[\"opportunity\"] >= 75 and (r[\"kd\"] or 0) <= 30 else \"REVIEW\"\n            ws2.append([template, r[\"entity\"], r[\"keyword\"], r[\"volume\"], r[\"kd\"] if r[\"kd\"] is not None else \"?\",\n                        r[\"opportunity\"], slug, status])\n\n    # --- Raw_Keywords (audit trail) ---\n    ws3 = wb.create_sheet(\"Raw_Keywords\")\n    headers3 = [\"Keyword\", \"Volume\", \"KD\", \"CPC\", \"Competition\", \"Trend\"]\n    write_header(ws3, headers3)\n    for r in rows:\n        ws3.append([r[\"keyword\"], r[\"volume\"], r[\"kd\"] if r[\"kd\"] is not None else \"?\", r.get(\"cpc\", \"\"), r.get(\"competition\", \"\"), r[\"trend\"]])\n\n    wb.save(out_path)\n\n\n# ---------------------------------------------------------------------------\n# Main\n# ---------------------------------------------------------------------------\n\ndef main():\n    ap = argparse.ArgumentParser(description=\"DataForSEO-powered SEO opportunity pipeline (v1)\")\n    ap.add_argument(\"--seed\", required=True, help='Seed keyword/topic, e.g. \"days until\"')\n    ap.add_argument(\"--site\", default=\"\", help=\"Your site URL (stored in output for reference only)\")\n    ap.add_argument(\"--country\", default=\"United States\")\n    ap.add_argument(\"--language\", default=\"English\")\n    ap.add_argument(\"--min-volume\", type=int, default=300)\n    ap.add_argument(\"--max-kd\", type=int, default=30)\n    ap.add_argument(\"--max-serp-calls\", type=int, default=40, help=\"Hard cap on expensive SERP calls per run\")\n    ap.add_argument(\"--dry-run\", action=\"store_true\", help=\"Show counts only, make no API calls\")\n    args = ap.parse_args()\n\n    print(f\"Seed: {args.seed!r} | Country: {args.country} | Language: {args.language}\")\n\n    if args.dry_run:\n        print(\"Dry run: would fetch keyword ideas, then filter by volume/KD, then SERP the top \"\n              f\"{args.max_serp_calls} survivors. No API calls made.\")\n        return\n\n    ideas = get_keyword_ideas(args.seed, args.country, args.language)\n    print(f\"Keyword ideas returned: {len(ideas)}\")\n\n    survivors = [i for i in ideas if i[\"volume\"] >= args.min_volume and (i[\"kd\"] is None or i[\"kd\"] <= args.max_kd)]\n    survivors.sort(key=lambda x: -x[\"volume\"])\n    print(f\"Survivors after volume/KD filter: {len(survivors)}\")\n\n    serp_batch = survivors[: args.max_serp_calls]\n    if len(survivors) > args.max_serp_calls:\n        print(f\"Capping SERP calls at {args.max_serp_calls} (of {len(survivors)} survivors) \u2014 raise --max-serp-calls to widen this.\")\n\n    keyword_domains = {}\n    for item in serp_batch:\n        keyword_domains[item[\"keyword\"]] = get_serp(item[\"keyword\"], args.country, args.language)\n\n    clusters = cluster_by_serp_overlap(keyword_domains)\n\n    rows = []\n    for item in survivors:\n        trend = trend_direction(item[\"monthly_searches\"])\n        has_serp = item[\"keyword\"] in keyword_domains\n        score = opportunity_score(item[\"volume\"], item[\"kd\"], trend, has_serp)\n        template, entity = detect_programmatic(item[\"keyword\"])\n        rows.append({\n            \"keyword\": item[\"keyword\"],\n            \"volume\": item[\"volume\"],\n            \"kd\": item[\"kd\"],\n            \"cpc\": item[\"cpc\"],\n            \"competition\": item[\"competition\"],\n            \"trend\": trend,\n            \"cluster\": clusters.get(item[\"keyword\"], \"\"),\n            \"opportunity\": score,\n            \"template\": template,\n            \"entity\": entity,\n            \"notes\": \"\" if has_serp else \"SERP not pulled (outside --max-serp-calls) \u2014 score discounted\",\n        })\n\n    out_dir = Path(\"seo_output\")\n    out_dir.mkdir(exist_ok=True)\n    timestamp = datetime.now().strftime(\"%Y%m%d_%H%M\")\n    out_path = out_dir / f\"opportunities_{timestamp}.xlsx\"\n    build_workbook(rows, out_path)\n    print(f\"\\nDone. Wrote {len(rows)} rows to {out_path}\")\n\n\nif __name__ == \"__main__\":\n    main()\n";
const README_CONTENT = "# SEO Opportunity Pipeline \u2014 v1\n\n## Files\n\n- **seo_pipeline_v1.py** \u2014 run this against your DataForSEO account. Outputs `seo_output/opportunities_<timestamp>.xlsx` with three tabs: `Opportunities` (scored keyword list), `Programmatic` (detected `days until [X]`-style templates, with a BUILD/REVIEW status), `Raw_Keywords` (audit trail of everything pulled).\n- **entities.schema.json** \u2014 the data contract your Next.js `ArticleLayout`/`EventLayout` should read from. Each row = one programmatic page.\n- **example_entity.json** \u2014 one worked example (Christmas) showing what a fully filled-in, publish-ready record looks like.\n\n## How they connect\n\n```\nseo_pipeline_v1.py\n      \u2502  (writes)\n      \u25bc\nProgrammatic tab in the .xlsx\n      \u2502  (you review \u2014 mark BUILD rows, write the uniqueContent.blurb/faq)\n      \u25bc\nentities.json  (array of objects matching entities.schema.json)\n      \u2502  (imported by the site build)\n      \u25bc\n/days-until/[slug]/  \u2014 one dynamic route, driven by entities.json\n```\n\nThe Excel \"Programmatic\" tab is a *review queue*, not the final data source. A row only becomes a live page once someone has filled in `uniqueContent.blurb` and at least 3 FAQ items \u2014 the schema makes `status: \"published\"` require those fields so a keyword with a great score can't accidentally ship as a thin, templated page.\n\n## Credentials\n\nThis script reads `DATAFORSEO_LOGIN` / `DATAFORSEO_PASSWORD` from `.env.local` at\nyour repo root (falling back to `.env`) \u2014 the same file Next.js uses, so you set\nthese once and they work locally on any machine you clone the repo onto. A real\nenvironment variable (set in your shell, or by a host like Netlify) always wins\nover the file, so the identical code path works for a deployed/triggered run too.\n\n`.env.example` has a placeholder block showing the exact variable names \u2014 copy\nthose into your real `.env.local`, and add the same two names (with real values)\nto your host's environment variables UI for any deployed/server-triggered run.\n\n## Running it\n\n```bash\npip install requests openpyxl python-dotenv\n\n# see what it would do without spending anything\npython scripts/seo-pipeline/seo_pipeline_v1.py --seed \"days until\" --dry-run\n\n# real run (credentials come from .env.local automatically)\npython scripts/seo-pipeline/seo_pipeline_v1.py \\\n  --seed \"days until\" \\\n  --site \"https://howlonguntilx.com\" \\\n  --country \"United States\" \\\n  --language English \\\n  --min-volume 300 \\\n  --max-kd 30 \\\n  --max-serp-calls 40\n```\n\n`--max-serp-calls` is your cost control \u2014 SERP calls are the expensive DataForSEO endpoint, so the script only spends on the top N keywords that already cleared the volume/KD filter. Raise it once you trust the pipeline; start low (20-40) while testing.\n\n## Known gaps in this v1 (deliberate \u2014 see chat)\n\n- Keyword-idea endpoint payload matches DataForSEO's documented v3 shape as of this writing, but DataForSEO does version its API \u2014 if you get a 4xx on first run, check `docs.dataforseo.com` for the current `dataforseo_labs/google/keyword_ideas/live` request schema and adjust the `payload` dict in `get_keyword_ideas()`.\n- No GSC integration yet (Phase 13 in our discussion) \u2014 add once you have live pages.\n- No AI-assisted question/FAQ generation yet \u2014 this is the natural next addition, likely as a step between the \"Programmatic\" tab and filling in `uniqueContent`.\n";
const SCHEMA_CONTENT = "{\n  \"$schema\": \"http://json-schema.org/draft-07/schema#\",\n  \"title\": \"ProgrammaticEntity\",\n  \"description\": \"One row = one programmatic page (e.g. /days-until/christmas/). This is the data contract between the SEO pipeline's 'Programmatic' tab and the site's dynamic route/layout component. Populate this from the pipeline output after human review, not automatically.\",\n  \"type\": \"object\",\n  \"required\": [\"slug\", \"template\", \"entityName\", \"targetDate\", \"status\", \"primaryKeyword\"],\n  \"properties\": {\n    \"slug\": { \"type\": \"string\", \"description\": \"URL segment, e.g. 'christmas'. Used to build /days-until/{slug}/\" },\n    \"template\": { \"type\": \"string\", \"enum\": [\"days-until\", \"weeks-until\", \"months-until\", \"countdown\"], \"description\": \"Which layout variant / URL pattern this entity belongs to\" },\n    \"entityName\": { \"type\": \"string\", \"description\": \"Display name, e.g. 'Christmas'\" },\n    \"category\": { \"type\": \"string\", \"description\": \"e.g. 'holiday', 'season', 'life-event', 'cultural' \u2014 used for related-entity grouping\" },\n    \"targetDate\": { \"type\": \"string\", \"format\": \"date\", \"description\": \"Next occurrence, ISO 8601. Recompute yearly for recurring events (build job, not manual edit)\" },\n    \"recurring\": { \"type\": \"boolean\", \"description\": \"True for annual events like Christmas; false for one-off dates\" },\n    \"status\": { \"type\": \"string\", \"enum\": [\"draft\", \"review\", \"published\", \"noindex\"], \"description\": \"Gates whether the route is statically generated / indexable. Only set to 'published' after the unique-content checklist below is satisfied.\" },\n    \"primaryKeyword\": { \"type\": \"string\" },\n    \"secondaryKeywords\": { \"type\": \"array\", \"items\": { \"type\": \"string\" } },\n    \"searchVolume\": { \"type\": \"integer\" },\n    \"keywordDifficulty\": { \"type\": \"integer\" },\n    \"opportunityScore\": { \"type\": \"number\" },\n    \"uniqueContent\": {\n      \"type\": \"object\",\n      \"description\": \"Fields that MUST be filled in per-entity to avoid thin/duplicate programmatic pages. If any required sub-field is empty, status cannot be 'published'.\",\n      \"required\": [\"blurb\", \"faq\"],\n      \"properties\": {\n        \"blurb\": { \"type\": \"string\", \"description\": \"2-4 sentences unique to this entity \u2014 why this date/event matters, not template boilerplate\" },\n        \"historicalNote\": { \"type\": \"string\", \"description\": \"Optional: origin/history/trivia specific to this entity\" },\n        \"faq\": {\n          \"type\": \"array\",\n          \"minItems\": 3,\n          \"items\": {\n            \"type\": \"object\",\n            \"required\": [\"question\", \"answer\"],\n            \"properties\": {\n              \"question\": { \"type\": \"string\" },\n              \"answer\": { \"type\": \"string\" }\n            }\n          }\n        }\n      }\n    },\n    \"relatedEntities\": { \"type\": \"array\", \"items\": { \"type\": \"string\" }, \"description\": \"Slugs of related entities for internal linking (same category or SERP cluster)\" },\n    \"images\": {\n      \"type\": \"array\",\n      \"maxItems\": 5,\n      \"items\": {\n        \"type\": \"object\",\n        \"required\": [\"filename\", \"alt\", \"purpose\"],\n        \"properties\": {\n          \"filename\": { \"type\": \"string\", \"description\": \"SEO-optimized, hyphenated, descriptive \u2014 e.g. christmas-countdown-days-until-christmas.jpg\" },\n          \"alt\": { \"type\": \"string\" },\n          \"purpose\": { \"type\": \"string\", \"enum\": [\"hero\", \"supporting\", \"explanatory\"] },\n          \"generationPrompt\": { \"type\": \"string\", \"description\": \"Prompt used with the image tool, kept for reproducibility\" }\n        }\n      }\n    },\n    \"schema\": {\n      \"type\": \"object\",\n      \"description\": \"Structured data to emit on the page\",\n      \"properties\": {\n        \"eventSchema\": { \"type\": \"boolean\", \"default\": true },\n        \"faqPageSchema\": { \"type\": \"boolean\", \"default\": true }\n      }\n    }\n  }\n}\n";
const EXAMPLE_CONTENT = "{\n  \"slug\": \"christmas\",\n  \"template\": \"days-until\",\n  \"entityName\": \"Christmas\",\n  \"category\": \"holiday\",\n  \"targetDate\": \"2026-12-25\",\n  \"recurring\": true,\n  \"status\": \"published\",\n  \"primaryKeyword\": \"days until christmas\",\n  \"secondaryKeywords\": [\"how many days until christmas\", \"christmas countdown\"],\n  \"searchVolume\": 18000,\n  \"keywordDifficulty\": 14,\n  \"opportunityScore\": 94.2,\n  \"uniqueContent\": {\n    \"blurb\": \"Christmas falls on December 25th every year and is one of the most searched countdowns globally, with interest climbing sharply from late November through the day itself.\",\n    \"historicalNote\": \"The December 25th date was fixed in the 4th century and has stayed constant even as the Gregorian calendar reform shifted other observances.\",\n    \"faq\": [\n      { \"question\": \"How many days until Christmas 2026?\", \"answer\": \"Use the live counter above \u2014 it updates automatically based on your local time zone.\" },\n      { \"question\": \"Is Christmas always on December 25th?\", \"answer\": \"Yes, in the Gregorian calendar used by most countries. Some Orthodox churches observe it on January 7th instead.\" },\n      { \"question\": \"How many weeks until Christmas?\", \"answer\": \"See the weeks-until conversion in the calculator above.\" }\n    ]\n  },\n  \"relatedEntities\": [\"new-year\", \"christmas-eve\", \"boxing-day\"],\n  \"images\": [\n    { \"filename\": \"christmas-countdown-days-until-christmas.jpg\", \"alt\": \"Christmas countdown showing days remaining until December 25th\", \"purpose\": \"hero\" },\n    { \"filename\": \"christmas-calendar-countdown.jpg\", \"alt\": \"Calendar illustrating how the Christmas countdown is calculated\", \"purpose\": \"explanatory\" }\n  ],\n  \"schema\": { \"eventSchema\": true, \"faqPageSchema\": true }\n}\n";
const PRISMA_MODEL = "\n// \u2500\u2500 SEO_PROGRAMMATIC_ENTITY_MODEL (added by setup-seo-pipeline.cjs) \u2500\u2500\n// NOTE: superseded once the Event model is confirmed to cover this use case\n// \u2014 see remove-programmatic-entity.sh in the same delivery batch.\nmodel ProgrammaticEntity {\n  id                 String   @id @default(cuid())\n  slug               String   @unique\n  template           String\n  entityName         String\n  category           String?\n  targetDate         DateTime\n  recurring          Boolean  @default(false)\n  status             String   @default(\"draft\")\n  primaryKeyword     String\n  secondaryKeywords  String[]\n  searchVolume       Int?\n  keywordDifficulty  Int?\n  opportunityScore   Float?\n  blurb              String?\n  historicalNote     String?\n  faq                Json?\n  relatedEntitySlugs String[]\n  images             Json?\n  createdAt          DateTime @default(now())\n  updatedAt          DateTime @updatedAt\n}\n";
const ENV_BLOCK = "\n# \u2500\u2500 SEO pipeline (DataForSEO) \u2014 added by setup-seo-pipeline.cjs \u2500\u2500\n# Used by scripts/seo-pipeline/seo_pipeline_v1.py, and by any future\n# server-triggered pipeline run. Add the SAME two names (with real values)\n# to your deployment host's environment variables UI (e.g. Netlify:\n# Site settings -> Environment variables) for a non-local run.\nDATAFORSEO_LOGIN=\nDATAFORSEO_PASSWORD=\n";

function writeFile(relPath, content) {
  const abs = path.join(ROOT, relPath);
  fs.mkdirSync(path.dirname(abs), { recursive: true });
  if (fs.existsSync(abs) && !FORCE) {
    console.log(`  SKIP  (exists) ${relPath}`);
    return;
  }
  fs.writeFileSync(abs, content, "utf8");
  console.log(`  WRITE ${relPath}`);
}

function appendOnceToFile(relPath, marker, block, header = "") {
  const abs = path.join(ROOT, relPath);
  if (!fs.existsSync(abs)) {
    console.log(`  SKIP  (${relPath} not found — create it first) `);
    return;
  }
  const current = fs.readFileSync(abs, "utf8");
  if (current.includes(marker)) {
    console.log(`  SKIP  (already present) ${relPath}`);
    return;
  }
  fs.appendFileSync(abs, (header ? "\n" + header + "\n" : "") + block + "\n", "utf8");
  console.log(`  APPEND ${relPath}`);
}

console.log("Creating SEO pipeline files...");

writeFile("scripts/seo-pipeline/seo_pipeline_v1.py", PY_CONTENT);
writeFile("scripts/seo-pipeline/README.md", README_CONTENT);
writeFile("docs/seo/entities.schema.json", SCHEMA_CONTENT);
writeFile("docs/seo/entities.example.json", EXAMPLE_CONTENT);

// keep the cache/output dirs tracked-but-empty in git
writeFile("scripts/seo-pipeline/cache/.gitkeep", "");
writeFile("scripts/seo-pipeline/seo_output/.gitkeep", "");

console.log("\nUpdating .gitignore...");
if (!fs.existsSync(path.join(ROOT, ".gitignore"))) {
  fs.writeFileSync(path.join(ROOT, ".gitignore"), "", "utf8");
}
appendOnceToFile(
  ".gitignore",
  "scripts/seo-pipeline/cache/",
  "scripts/seo-pipeline/cache/\nscripts/seo-pipeline/seo_output/",
  "# SEO pipeline (added by setup-seo-pipeline.cjs)"
);

console.log("\nUpdating .env.example...");
if (!fs.existsSync(path.join(ROOT, ".env.example"))) {
  fs.writeFileSync(path.join(ROOT, ".env.example"), "", "utf8");
  console.log("  (created .env.example — none existed)");
}
appendOnceToFile(
  ".env.example",
  "SEO pipeline (DataForSEO)",
  ENV_BLOCK.trim()
);

console.log("\nUpdating prisma/schema.prisma...");
appendOnceToFile(
  "prisma/schema.prisma",
  "SEO_PROGRAMMATIC_ENTITY_MODEL",
  PRISMA_MODEL
);

console.log("\nDone. Next steps:");
console.log("  1. Copy the DATAFORSEO_LOGIN / DATAFORSEO_PASSWORD lines from .env.example");
console.log("     into your real .env.local, and fill in the actual values.");
console.log("  2. For a deployed/server-triggered run: add the same two names + real");
console.log("     values in Netlify -> Site settings -> Environment variables.");
console.log("  3. If prisma/schema.prisma was updated: npx prisma migrate dev --name add_programmatic_entity");
console.log("  4. pip install requests openpyxl python-dotenv");
console.log("  5. python scripts/seo-pipeline/seo_pipeline_v1.py --seed \"days until\" --dry-run");
